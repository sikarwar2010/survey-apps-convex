# Convex backend (read-only mirror)

**Canonical source:** `../sdv-front-new-app/convex/`

Edit backend functions only in the web repo, then sync here for mobile TypeScript types:

```bash
npm run sync:convex
```

Run `convex dev` and `convex deploy` only from `../sdv-front-new-app`.
